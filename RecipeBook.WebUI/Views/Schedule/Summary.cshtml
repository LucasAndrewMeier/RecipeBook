@model RecipeBook.Domain.Entities.Schedule

<div class="navbar-right">
    @Html.ActionLink("Checkout","Index","Schedule", new { returnUrl = Request.Url.PathAndQuery }, new {@class = "btn btn-default navbar-btn"})
</div>

<div class="navbar-text navbar-right">
    <b>Schedule QuickView:</b>
    @Model.Lines.Select(x => x.Recipe)
</div>